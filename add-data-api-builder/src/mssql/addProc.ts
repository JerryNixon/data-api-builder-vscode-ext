import * as vscode from 'vscode';

export async function addProc(configPath: string, connectionString: string) {
  vscode.window.showInformationMessage(`Adding a stored procedure to ${configPath}`);
  // TODO: Implement the logic for adding a stored procedure.
}
