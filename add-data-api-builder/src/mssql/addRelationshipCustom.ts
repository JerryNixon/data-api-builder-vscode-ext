import * as vscode from 'vscode';

/**
 * Placeholder for adding custom (ad hoc) relationships to the configuration.
 * @param configPath - The path to the configuration file.
 */
export async function addRelationshipCustom(configPath: string) {
  vscode.window.showInformationMessage('Custom relationship support is not yet implemented.');
}
